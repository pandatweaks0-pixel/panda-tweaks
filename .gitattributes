# Normalise line endings in the repo; check out native on each platform.
# Without this every Windows edit rewrites whole files as CRLF and the diffs
# stop showing what actually changed.
* text=auto
*.ps1 text eol=crlf
